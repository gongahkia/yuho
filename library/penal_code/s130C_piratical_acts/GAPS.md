## GA

Statute text: "shall be liable to caning"

The current Yuho grammar requires `caning` to be encoded with a numeric stroke count. Section 130C imposes liability to caning without specifying any minimum or maximum number of strokes, so the caning limb is preserved verbatim in `supplementary` rather than fabricated as a numeric `caning := ... strokes` clause.
