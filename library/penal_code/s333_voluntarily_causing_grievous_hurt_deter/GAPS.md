## GA

Statute text: "shall also be liable to fine or to caning."

The current grammar can encode `fine := unlimited`, but `caning` requires an explicit stroke count or range. Section 333 does not specify a number of strokes, so the uncapped caning liability is preserved only in `supplementary` text.
