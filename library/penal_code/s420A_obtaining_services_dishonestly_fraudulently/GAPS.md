## GA

Canonical `_raw/act.json` text for subsection `(1)` is truncated after:

> `( c ) when the person obtains the services —`

I encoded only the preserved content from subsection `(1)` and did not invent the missing paragraph `(c)` limb.
