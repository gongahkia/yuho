# Yuho does not currently ship a REST/OpenAPI server.
#
# Historical drafts of this file described a `yuho api --port 8080`
# surface. That command is not part of the current CLI. Use the Python
# package, `yuho` CLI, LSP (`yuho lsp`), or MCP server (`yuho serve`) instead.
