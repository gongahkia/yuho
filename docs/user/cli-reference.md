# Yuho CLI reference

_Auto-generated by `scripts/gen_cli_reference.py` on 2026-06-22._
_Do not edit by hand — regenerate after every CLI change._

This document mirrors the output of `yuho <subcommand> --help` for every implemented CLI command. The generator walks Click's command tree directly, so the doc and the binary cannot disagree.

## Command summary

| Command | Description |
|---|---|
| `yuho ast` | Visualize the parsed AST. |
| `yuho check` | Parse and validate a Yuho source file. |
| `yuho completion` | Generate shell completion script. |
| `yuho debug` | Debug statute evaluation against facts. |
| `yuho diff` | Compare Yuho statute files or corpus sections. |
| `yuho explain` | Explain element-by-element statute satisfaction. |
| `yuho fmt` | Format a Yuho source file. |
| `yuho irac` | Emit IRAC-structured English for a statute and facts. |
| `yuho lint` | Run statute-core lint and fidelity diagnostics. |
| `yuho refs` | Query the checked-in corpus reference graph. |
| `yuho schema` | Print JSON Schema for the retained JSON transpiler output. |
| `yuho test` | Run legal tests embedded in Yuho files. |
| `yuho transpile` | Transpile a Yuho source file. |
| `yuho upgrade` | Rewrite Yuho source to the requested grammar version. |
| `yuho verify` | Run retained formal verification backends. |

---

## Per-command details

## `yuho ast`

Visualize the parsed AST.

```
Usage: yuho ast [OPTIONS] FILE

  Visualize the parsed AST.

Options:
  -o, --output PATH  Output file path
  --stats            Show AST statistics
  --depth INTEGER    Max depth; 0 means unlimited
  --ascii            Use ASCII-only tree output
  --help             Show this message and exit.
```

## `yuho check`

Parse and validate a Yuho source file.

```
Usage: yuho check [OPTIONS] FILE

  Parse and validate a Yuho source file.

Options:
  --json                      Output errors as JSON
  --explain-error             Explain diagnostics
  --metrics                   Include code_scale and clock_load_scale metrics
  --watch                     Re-run when FILE changes
  --format [text|json|sarif]  Output format
  --syntax-only               Skip semantic analysis
  --feature [civil]           Enable experimental language feature
  --help                      Show this message and exit.
```

### Watch mode

`yuho check --watch PATH` runs an initial check, then keeps a watchdog observer on `PATH`'s parent directory and re-runs when that exact `.yh` file is created, modified, or moved. It is not recursive; unrelated events in `.git`, `node_modules`, `__pycache__`, or other files do not trigger a re-check.

```bash
yuho check --watch library/penal_code/s415_cheating/statute.yh
```

## `yuho completion`

Generate shell completion script.

```
Usage: yuho completion [OPTIONS] {bash|zsh|fish}

  Generate shell completion script.

Options:
  --install  Show installation instructions
  --help     Show this message and exit.
```

## `yuho debug`

Debug statute evaluation against facts.

```
Usage: yuho debug [OPTIONS] FACTS_FILE STATUTE_FILE

  Debug statute evaluation against facts.

Options:
  --break-on [element]  Breakpoint target  [required]
  --json                Emit JSON
  --help                Show this message and exit.
```

## `yuho diff`

Compare Yuho statute files or corpus sections.

```
Usage: yuho diff [OPTIONS] [TARGETS]...

  Compare Yuho statute files or corpus sections.

Options:
  --json                Output as JSON
  --score               Score file2 against file1
  --jurisdictions TEXT  Comma-separated jurisdiction codes for corpus section diff, e.g.
                        sg,my,pk
  --help                Show this message and exit.
```

## `yuho explain`

Explain element-by-element statute satisfaction.

```
Usage: yuho explain [OPTIONS] SECTION

  Explain element-by-element statute satisfaction.

Options:
  --facts PATH       [required]
  --library PATH     Library root
  --json             Emit JSON
  --feature [civil]  Enable experimental language feature
  --help             Show this message and exit.
```

## `yuho fmt`

Format a Yuho source file.

```
Usage: yuho fmt [OPTIONS] FILE

  Format a Yuho source file.

Options:
  -i, --in-place  Format file in place
  --check         Check formatting
  --help          Show this message and exit.
```

## `yuho irac`

Emit IRAC-structured English for a statute and facts.

```
Usage: yuho irac [OPTIONS] STATUTE_FILE

  Emit IRAC-structured English for a statute and facts.

Options:
  --facts PATH       [required]
  --feature [civil]  Enable experimental language feature
  --help             Show this message and exit.
```

## `yuho lint`

Run statute-core lint and fidelity diagnostics.

```
Usage: yuho lint [OPTIONS] FILES...

  Run statute-core lint and fidelity diagnostics.

Options:
  -r, --rule TEXT             Specific rules to run
  -e, --exclude TEXT          Rules to exclude
  --json                      Output as JSON
  --fix                       Auto-fix issues where supported
  --format [text|json|sarif]  Output format
  --help                      Show this message and exit.
```

## `yuho refs`

Query the checked-in corpus reference graph.

```
Usage: yuho refs [OPTIONS] [SECTION]

  Query the checked-in corpus reference graph.

Options:
  --library PATH                  Library root
  --in                            Only show incoming edges
  --out                           Only show outgoing edges
  --kind [subsumes|amends|implicit|authority|treatment_followed|treatment_distinguished|treatment_overruled]
                                  Filter edge kind
  --treatment                     Query case-law treatment edges
  --overruled                     Query overruled case-law edges
  --transitive                    Follow edges transitively
  --graph                         Print full graph summary
  --scc                           Run SCC analysis
  --json                          Emit JSON
  --help                          Show this message and exit.
```

## `yuho schema`

Print JSON Schema for the retained JSON transpiler output.

```
Usage: yuho schema [OPTIONS]

  Print JSON Schema for the retained JSON transpiler output.

Options:
  -o, --output PATH  Output file path
  --help             Show this message and exit.
```

## `yuho test`

Run legal tests embedded in Yuho files.

```
Usage: yuho test [OPTIONS] [FILE]

  Run legal tests embedded in Yuho files.

Options:
  --all                       Run all tests in directory
  --json                      Output results as JSON
  --coverage                  Enable coverage tracking
  --coverage-html PATH        Generate HTML coverage report
  --format [text|json|junit]  Output format
  --help                      Show this message and exit.
```

## `yuho transpile`

Transpile a Yuho source file.

```
Usage: yuho transpile [OPTIONS] FILE

  Transpile a Yuho source file.

Options:
  -t, --target [json|english|latex|pdf|mermaid|mindmap|mermaid-mindmap|svg|png|alloy|docx|akomantoso|akn|legaldocml|legalruleml|lrml]
                                  Transpilation target format
  -o, --output PATH               Output file path
  --dir PATH                      Output directory
  --all                           Generate all retained targets
  --shape [statute|schema|verbose]
                                  Mermaid flowchart shape
  --json                          Output metadata as JSON
  --help                          Show this message and exit.
```

### Transpile targets

| Base target | Extension | Aliases | Output |
|---|---:|---|---|
| `json` | `.json` | - | Structured JSON representation. |
| `english` | `.txt` | en | Controlled English rendering. |
| `latex` | `.tex` | tex | LaTeX source. |
| `mermaid` | `.mmd` | mmd | Mermaid flowchart source. |
| `mindmap` | `.mmd` | mermaid-mindmap | Mermaid mindmap source. |
| `alloy` | `.als` | - | Alloy model source. |
| `docx` | `.docx` | word | Word document. |
| `akomantoso` | `.xml` | akn, legaldocml | Akoma Ntoso XML. |
| `legalruleml` | `.lrml` | lrml | LegalRuleML XML. |

### Derived outputs

`pdf`, `svg`, and `png` are not registered base transpilers. They are derived by external renderers from `latex` (PDF) or `mermaid` (SVG/PNG).

## `yuho upgrade`

Rewrite Yuho source to the requested grammar version.

```
Usage: yuho upgrade [OPTIONS] FILE

  Rewrite Yuho source to the requested grammar version.

Options:
  -i, --in-place  Rewrite FILE in place
  --check         Exit 1 if FILE needs upgrade
  --from TEXT     Expected source grammar version
  --to TEXT       Target grammar version  [default: 5.1]
  --help          Show this message and exit.
```

## `yuho verify`

Run retained formal verification backends.

```
Usage: yuho verify [OPTIONS] [FILE]

  Run retained formal verification backends.

Options:
  --engine [combined|alloy|z3]  Verification backend  [default: combined]
  --alloy-jar PATH              Path to Alloy analyzer JAR
  --alloy-timeout INTEGER       [default: 30]
  --z3-timeout-ms INTEGER       [default: 5000]
  --capabilities                Show backend status
  --json                        Output as JSON
  --help                        Show this message and exit.
```

---

## Global options

```
Usage: yuho [OPTIONS] COMMAND [ARGS]...

  Yuho - statute-core DSL compiler and corpus tooling.

Options:
  --version             Show the version and exit.
  -v, --verbose         Enable verbose output
  --color / --no-color  Force color output
  -q, --quiet           Suppress non-error output
  --help                Show this message and exit.

Commands:
  ast         Visualize the parsed AST.
  check       Parse and validate a Yuho source file.
  completion  Generate shell completion script.
  debug       Debug statute evaluation against facts.
  diff        Compare Yuho statute files or corpus sections.
  explain     Explain element-by-element statute satisfaction.
  fmt         Format a Yuho source file.
  irac        Emit IRAC-structured English for a statute and facts.
  lint        Run statute-core lint and fidelity diagnostics.
  refs        Query the checked-in corpus reference graph.
  schema      Print JSON Schema for the retained JSON transpiler output.
  test        Run legal tests embedded in Yuho files.
  transpile   Transpile a Yuho source file.
  upgrade     Rewrite Yuho source to the requested grammar version.
  verify      Run retained formal verification backends.
```
