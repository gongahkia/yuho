# Yuho CLI reference

_Auto-generated by `scripts/gen_cli_reference.py` on 2026-04-26._
_Do not edit by hand — regenerate after every CLI change._

This document mirrors the output of `yuho <subcommand> --help` for every implemented CLI command. The generator walks Click's command tree directly, so the doc and the binary cannot disagree.

## Command summary

| Command | Description |
|---|---|
| `yuho ast` | Visualize AST structure as tree. |
| `yuho batch` | Batch operations on directories of .yh files. |
| `yuho check` | Parse and validate a Yuho source file. |
| `yuho ci-report` | Run check+lint on all .yh files and produce a unified report. |
| `yuho completion` | Generate shell completion script. |
| `yuho compliance-matrix` | Generate a compliance checklist from statute elements. |
| `yuho config` | Manage Yuho configuration. |
| `yuho contribute` | Validate a statute file for contribution to the library. |
| `yuho deps` | Show statute dependencies for a Yuho file. |
| `yuho diff` | Compare two Yuho files and show semantic differences. |
| `yuho eval` | Evaluate a Yuho file through the interpreter. |
| `yuho explain` | Plain-language summary of an encoded statute. |
| `yuho explore` | Counter-example explorer: surface fact patterns over a section. |
| `yuho fmt` | Format a Yuho source file. |
| `yuho generate` | Generate statute scaffold with proper structure. |
| `yuho generate-tests` | Generate test cases using Z3 constraint solving. |
| `yuho graph` | Visualize statute dependencies as a graph. |
| `yuho init` | Initialize a new Yuho statute project. |
| `yuho library` | Manage Yuho statute packages. |
| `yuho lint` | Check Yuho files for style and best practice issues. |
| `yuho lsp` | Start the Language Server Protocol server. |
| `yuho recommend` | Charge-recommender: rank Penal Code sections that structurally fit a fact pattern. |
| `yuho refs` | Query the cross-section reference graph (G10). |
| `yuho repl` | Start interactive REPL for statute experimentation. |
| `yuho schema` | Print JSON Schema for Yuho JSON transpiler output. |
| `yuho serve` | Start the MCP (Model Context Protocol) server. |
| `yuho test` | Run tests for a Yuho statute file. |
| `yuho transpile` | Transpile a Yuho source file to another format. |
| `yuho verify` | Run formal verification checks (Alloy, Z3, or combined). |
| `yuho verify-report` | Generate a LaTeX verification report for statute completeness. |
| `yuho wizard` | Interactive wizard for statute creation. |

---

## Per-command details

## `yuho ast`

Visualize AST structure as tree.

```
Usage: yuho ast [OPTIONS] FILE

  Visualize AST structure as tree.

  Displays the abstract syntax tree of a Yuho statute using tree-like ASCII art in the
  terminal.

  Examples:     yuho ast statute.yh     yuho ast statute.yh --stats     yuho ast
  statute.yh --ascii -o tree.txt

Options:
  -o, --output PATH  Output file path
  --tree             Show tree visualization (default)
  --stats            Show AST statistics
  --depth INTEGER    Max depth (0 = unlimited)
  --ascii            Use ASCII-only characters
  --help             Show this message and exit.
```

## `yuho batch`

Batch operations on directories of .yh files.

```
Usage: yuho batch [OPTIONS] COMMAND [ARGS]...

  Batch operations on directories of .yh files.

  Process multiple statute files at once for validation, transpilation, or testing.

Options:
  --help  Show this message and exit.

Commands:
  check      Check all .yh files in a directory.
  transpile  Transpile all .yh files in a directory.
```

### `yuho batch check`

Check all .yh files in a directory.

```
Usage: yuho batch check [OPTIONS] DIRECTORY

  Check all .yh files in a directory.

  Examples:     yuho batch check library/penal_code/     yuho batch check . --json

Options:
  --no-recursive  Don't recurse into subdirectories
  --json          Output as JSON
  --help          Show this message and exit.
```

### `yuho batch transpile`

Transpile all .yh files in a directory.

```
Usage: yuho batch transpile [OPTIONS] DIRECTORY

  Transpile all .yh files in a directory.

  Examples:     yuho batch transpile library/ -t json -o ./output/     yuho batch
  transpile . -t english

Options:
  -t, --target [json|english|latex|mermaid|alloy|docx]
                                  Transpilation target
  -o, --output-dir PATH           Output directory
  --no-recursive                  Don't recurse into subdirectories
  --json                          Output as JSON
  --help                          Show this message and exit.
```

## `yuho check`

Parse and validate a Yuho source file.

```
Usage: yuho check [OPTIONS] FILE

  Parse and validate a Yuho source file.

  Runs syntax checking and semantic analysis, reporting any errors found.

Options:
  --json                      Output errors as JSON
  --explain-error             Show detailed explanations for errors with common causes
                              and fixes
  --metrics                   Include code_scale and clock_load_scale metrics
  --format [text|json|sarif]  Output format
  --syntax-only               Skip semantic analysis and report only parse/AST validity
  --help                      Show this message and exit.
```

## `yuho ci-report`

Run check+lint on all .yh files and produce a unified report.

```
Usage: yuho ci-report [OPTIONS] [DIRECTORY]

  Run check+lint on all .yh files and produce a unified report.

Options:
  -o, --output PATH            Output file path
  --format [json|sarif|junit]  Output format
  --help                       Show this message and exit.
```

## `yuho completion`

Generate shell completion script.

```
Usage: yuho completion [OPTIONS] {bash|zsh|fish}

  Generate shell completion script.

  Outputs a completion script for the specified shell that can be sourced or saved to
  the appropriate location.

  Examples:     # Bash: Add to ~/.bashrc     eval "$(yuho completion bash)"

      # Zsh: Add to ~/.zshrc     eval "$(yuho completion zsh)"

      # Fish: Save to completions directory     yuho completion fish >
      ~/.config/fish/completions/yuho.fish

  Use --install to see detailed installation instructions.

Options:
  --install  Show installation instructions
  --help     Show this message and exit.
```

## `yuho compliance-matrix`

Generate a compliance checklist from statute elements.

```
Usage: yuho compliance-matrix [OPTIONS] FILE

  Generate a compliance checklist from statute elements.

  Produces a table of all statutory requirements with checkboxes for compliance officers
  to audit.

  Examples:     yuho compliance-matrix statute.yh     yuho compliance-matrix statute.yh
  -o matrix.md     yuho compliance-matrix statute.yh --json

Options:
  -o, --output PATH  Output file path
  --json             Output as JSON
  --help             Show this message and exit.
```

## `yuho config`

Manage Yuho configuration.

```
Usage: yuho config [OPTIONS] COMMAND [ARGS]...

  Manage Yuho configuration.

  View and modify configuration settings for all Yuho components.

Options:
  --help  Show this message and exit.

Commands:
  init  Create a default configuration file.
  set   Set a configuration value.
  show  Display current configuration.
```

### `yuho config init`

Create a default configuration file.

```
Usage: yuho config init [OPTIONS]

  Create a default configuration file.

  Creates ~/.config/yuho/config.toml with sensible defaults.

Options:
  --force  Overwrite existing config file
  --help   Show this message and exit.
```

### `yuho config set`

Set a configuration value.

```
Usage: yuho config set [OPTIONS] KEY VALUE

  Set a configuration value.

  KEY must be in format 'section.key' (e.g., 'llm.provider').

  Examples:     yuho config set llm.provider ollama     yuho config set llm.model llama3
  yuho config set mcp.port 9000

Options:
  --help  Show this message and exit.
```

### `yuho config show`

Display current configuration.

```
Usage: yuho config show [OPTIONS]

  Display current configuration.

  Shows all configuration values from file and environment.

  Examples:     yuho config show     yuho config show -s llm     yuho config show -f
  json

Options:
  -s, --section [llm|transpile|lsp|mcp|library]
                                  Show only specific section
  -f, --format [toml|json]        Output format
  --help                          Show this message and exit.
```

## `yuho contribute`

Validate a statute file for contribution to the library.

```
Usage: yuho contribute [OPTIONS] FILE

  Validate a statute file for contribution to the library.

  Checks that the file parses correctly and has associated tests.

Options:
  --package          Create distributable .yhpkg archive
  -o, --output PATH  Output path for package
  --help             Show this message and exit.
```

## `yuho deps`

Show statute dependencies for a Yuho file.

```
Usage: yuho deps [OPTIONS] FILE

  Show statute dependencies for a Yuho file.

  Lists all imports, references, and subsumption relations.

  Examples:     yuho deps statute.yh     yuho deps statute.yh --json

Options:
  --json  Output as JSON
  --help  Show this message and exit.
```

## `yuho diff`

Compare two Yuho files and show semantic differences.

```
Usage: yuho diff [OPTIONS] FILE1 FILE2

  Compare two Yuho files and show semantic differences.

  Shows added, removed, and modified statutes, definitions, elements, penalties, and
  illustrations between two versions.

  Use --score to grade file2 against file1 as a model answer, showing element coverage
  percentage.

  Examples:     yuho diff old.yh new.yh     yuho diff v1/statute.yh v2/statute.yh --json
  yuho diff model.yh student.yh --score

Options:
  --json   Output as JSON
  --score  Score file2 against file1 as model answer
  --help   Show this message and exit.
```

## `yuho eval`

Evaluate a Yuho file through the interpreter.

```
Usage: yuho eval [OPTIONS] FILE

  Evaluate a Yuho file through the interpreter.

  Parses, analyzes, and evaluates the file, reporting registered statutes, structs,
  functions, and bindings.

  Examples:     yuho eval statute.yh     yuho eval statute.yh --json

Options:
  --json  Output as JSON
  --help  Show this message and exit.
```

## `yuho explain`

Plain-language summary of an encoded statute.

```
Usage: yuho explain [OPTIONS] FILE [SECTION]

  Plain-language summary of an encoded statute.

  Renders a 5-section prose block: title, what-it-covers (from metadata.toml summary),
  elements, penalty, and a worked example (first illustration). With a section number it
  filters to that section; without one, it explains every statute in the file.

  Examples:

      yuho explain library/penal_code/s415_cheating/statute.yh     yuho explain
      library/penal_code/s415_cheating/statute.yh 415

Options:
  --help  Show this message and exit.
```

## `yuho explore`

Counter-example explorer: surface fact patterns over a section.

```
Usage: yuho explore [OPTIONS] FILE SECTION

  Counter-example explorer: surface fact patterns over a section.

  Reuses the Z3 verifier to enumerate (a) satisfying scenarios where the section's
  elements are all met, (b) borderline scenarios showing which elements are load-
  bearing, and (c) which exceptions are reachable.

  With --subsume <n>, compares the two sections and reports their relation (disjoint /
  overlap / one subsumes the other / equal), along with witness fact-bindings for each
  direction.

  Examples:     yuho explore library/penal_code/s415_cheating/statute.yh 415     yuho
  explore module.yh 415 --subsume 420 --json

Options:
  --max-satisfying INTEGER  Cap on satisfying scenarios returned  [default: 5]
  --no-borderline           Skip the per-element load-bearing analysis
  --no-exceptions           Skip the exception coverage / dead-exception scan
  --subsume TEXT            Compare with another section: report disjoint / overlap /
                            subsumes / equal relation. Pass the other section number.
  --json                    Output as JSON
  --help                    Show this message and exit.
```

## `yuho fmt`

Format a Yuho source file.

```
Usage: yuho fmt [OPTIONS] FILE

  Format a Yuho source file.

  Applies canonical formatting to the file.

Options:
  -i, --in-place  Format file in place
  --check         Check if file is formatted (exit 1 if not)
  --help          Show this message and exit.
```

## `yuho generate`

Generate statute scaffold with proper structure.

```
Usage: yuho generate [OPTIONS] SECTION

  Generate statute scaffold with proper structure.

  Creates a boilerplate .yh file with the specified section number and title. Fill in
  the TODO markers to complete your statute.

  Examples:     yuho generate 500 --title "Theft"     yuho generate s299 --title
  "Culpable Homicide" --template full     yuho generate 420 -t "Cheating" -o cheating.yh

Options:
  -t, --title TEXT                Statute title  [required]
  -o, --output PATH               Output file path
  --template [standard|minimal|full]
                                  Scaffold template type
  --no-definitions                Skip definitions block
  --no-penalty                    Skip penalty block
  --no-illustrations              Skip illustrations block
  -f, --force                     Overwrite existing file
  --help                          Show this message and exit.
```

## `yuho generate-tests`

Generate test cases using Z3 constraint solving.

```
Usage: yuho generate-tests [OPTIONS] FILE

  Generate test cases using Z3 constraint solving.

  Uses Z3 model enumeration to produce concrete fact patterns that exercise different
  code paths through statute elements.

  Examples:     yuho generate-tests statute.yh     yuho generate-tests statute.yh -n 20
  --json -o tests.json

Options:
  -o, --output PATH        Output file path
  -n, --max-cases INTEGER  Maximum test cases to generate  [default: 10]
  --json                   Output as JSON
  --help                   Show this message and exit.
```

## `yuho graph`

Visualize statute dependencies as a graph.

```
Usage: yuho graph [OPTIONS] FILE

  Visualize statute dependencies as a graph.

  Generates a dependency graph showing: - Statute cross-references - Import
  relationships - Type and function definitions

  Examples:     yuho graph statute.yh     yuho graph statute.yh --format dot -o deps.dot
  yuho graph statute.yh --format mermaid > deps.md

Options:
  -f, --format [dot|mermaid]  Output format
  -o, --output PATH           Output file path
  --help                      Show this message and exit.
```

## `yuho init`

Initialize a new Yuho statute project.

```
Usage: yuho init [OPTIONS] [NAME]

  Initialize a new Yuho statute project.

  Creates a directory structure with template files.

Options:
  -d, --dir PATH  Directory to create project in
  --help          Show this message and exit.
```

## `yuho library`

Manage Yuho statute packages.

```
Usage: yuho library [OPTIONS] COMMAND [ARGS]...

  Manage Yuho statute packages.

  Search, install, update, and manage statute packages from the library.

Options:
  --help  Show this message and exit.

Commands:
  export     Bulk-export all library statutes to a target format.
  info       Show detailed package information.
  install    Install a package.
  list       List all installed packages.
  outdated   Show packages with updates available.
  publish    Publish a package to the registry.
  search     Search for packages in the library.
  tree       Show dependency tree for packages.
  uninstall  Uninstall a package.
  update     Update one or all packages.
```

### `yuho library export`

Bulk-export all library statutes to a target format.

```
Usage: yuho library export [OPTIONS]

  Bulk-export all library statutes to a target format.

  Transpiles every statute.yh in the library/ directory and writes output preserving the
  directory structure.

  Examples:     yuho library export     yuho library export -t english -o
  ./docs/statutes     yuho library export -t mermaid --json

Options:
  -t, --target [json|english|latex|mermaid|alloy|docx]
                                  Transpilation target
  -o, --output-dir PATH           Output directory
  --json                          Output as JSON
  --help                          Show this message and exit.
```

### `yuho library info`

Show detailed package information.

```
Usage: yuho library info [OPTIONS] PACKAGE

  Show detailed package information.

  Examples:     yuho library info S403

Options:
  --json  Output as JSON
  --help  Show this message and exit.
```

### `yuho library install`

Install a package.

```
Usage: yuho library install [OPTIONS] PACKAGE

  Install a package.

  Examples:     yuho library install S403     yuho library install ./my-package.yhpkg

Options:
  -f, --force  Overwrite existing
  --no-deps    Skip dependencies
  --json       Output as JSON
  --help       Show this message and exit.
```

### `yuho library list`

List all installed packages.

```
Usage: yuho library list [OPTIONS]

  List all installed packages.

Options:
  --json  Output as JSON
  --help  Show this message and exit.
```

### `yuho library outdated`

Show packages with updates available.

```
Usage: yuho library outdated [OPTIONS]

  Show packages with updates available.

  Displays installed packages that have newer versions in the registry, along with the
  type of update (major, minor, patch) and deprecation warnings.

  Examples:     yuho library outdated     yuho library outdated --json

Options:
  --json  Output as JSON
  --help  Show this message and exit.
```

### `yuho library publish`

Publish a package to the registry.

```
Usage: yuho library publish [OPTIONS] PATH

  Publish a package to the registry.

  Examples:     yuho library publish ./my-statute --token $YUHO_TOKEN     yuho library
  publish ./my-statute --dry-run

Options:
  --registry TEXT  Registry URL
  --token TEXT     Auth token
  --dry-run        Validate package without publishing
  --json           Output as JSON
  --help           Show this message and exit.
```

### `yuho library search`

Search for packages in the library.

```
Usage: yuho library search [OPTIONS] QUERY

  Search for packages in the library.

  Examples:     yuho library search theft     yuho library search S403 --jurisdiction
  singapore

Options:
  -j, --jurisdiction TEXT  Filter by jurisdiction
  -t, --tag TEXT           Filter by tag
  -n, --limit INTEGER      Max results
  --json                   Output as JSON
  --help                   Show this message and exit.
```

### `yuho library tree`

Show dependency tree for packages.

```
Usage: yuho library tree [OPTIONS] [PACKAGE]

  Show dependency tree for packages.

  Displays a tree visualization of package dependencies. If no package is specified,
  shows trees for all installed packages.

  Examples:     yuho library tree     yuho library tree S403     yuho library tree
  --depth 3

Options:
  -d, --depth INTEGER  Maximum depth to display
  --json               Output as JSON
  --help               Show this message and exit.
```

### `yuho library uninstall`

Uninstall a package.

```
Usage: yuho library uninstall [OPTIONS] PACKAGE

  Uninstall a package.

  Examples:     yuho library uninstall S403

Options:
  --dry-run  Show what would be done
  --json     Output as JSON
  --help     Show this message and exit.
```

### `yuho library update`

Update one or all packages.

```
Usage: yuho library update [OPTIONS] [PACKAGE]

  Update one or all packages.

  Without arguments, shows available updates. Use --all to apply all updates.

  Examples:     yuho library update           # Show updates     yuho library update
  --all     # Apply all updates     yuho library update S403      # Update specific
  package

Options:
  --all   Update all packages
  --json  Output as JSON
  --help  Show this message and exit.
```

## `yuho lint`

Check Yuho files for style and best practice issues.

```
Usage: yuho lint [OPTIONS] FILES...

  Check Yuho files for style and best practice issues.

  Analyzes files for: - Missing statute titles, elements, or penalties - Naming
  convention violations - Unused definitions - Duplicate section numbers

  Examples:     yuho lint statute.yh     yuho lint *.yh --exclude missing-penalty
  yuho lint src/ --json     yuho lint src/ --format sarif

Options:
  -r, --rule TEXT             Specific rules to run
  -e, --exclude TEXT          Rules to exclude
  --json                      Output as JSON
  --fix                       Auto-fix issues where possible
  --format [text|json|sarif]  Output format
  --help                      Show this message and exit.
```

## `yuho lsp`

Start the Language Server Protocol server.

```
Usage: yuho lsp [OPTIONS]

  Start the Language Server Protocol server.

  For editor integration (VS Code, Neovim, etc.).

Options:
  --tcp INTEGER  Start LSP server on TCP port
  --workspace    On startup, walk every .yh file in each workspace folder and publish
                 diagnostics. Editor's 'problems' panel will show issues from the entire
                 library, not just open documents.
  --help         Show this message and exit.
```

## `yuho recommend`

Charge-recommender: rank Penal Code sections that structurally fit a fact pattern.

```
Usage: yuho recommend [OPTIONS] FACTS_PATH

  Charge-recommender: rank Penal Code sections that structurally fit a fact pattern.

  NOT LEGAL ADVICE — every output carries an explicit disclaimer. The recommender re-
  uses the existing fact-pattern simulator for per-element traces, then sorts by
  coverage. No external LLM, no scraping.

  Example:     yuho recommend simulator/fixtures/s415_classic.yaml

Options:
  --top-k INTEGER           How many ranked candidates to return  [default: 5]
  --max-candidates INTEGER  Cap on sections whose simulator trace gets run  [default:
                            60]
  --min-coverage FLOAT      Drop candidates below this coverage fraction (0.0 .. 1.0)
                            [default: 0.0]
  --json                    Output as JSON
  --help                    Show this message and exit.
```

## `yuho refs`

Query the cross-section reference graph (G10).

```
Usage: yuho refs [OPTIONS] [SECTION]

  Query the cross-section reference graph (G10).

  With a section number, prints the edges entering and/or leaving that section. Without
  a section, prints aggregate stats; pass --graph to dump the full graph.

  Examples:     yuho refs s415               # in + out for s415     yuho refs s415
  --out --transitive     yuho refs s415 --kind subsumes     yuho refs
  # full-graph stats     yuho refs --graph            # full graph summary     yuho refs
  s415 --json

Options:
  --library PATH                  Library root (default: library/penal_code)
  --in                            Only show edges entering the section
  --out                           Only show edges leaving the section
  --kind [subsumes|amends|implicit]
                                  Filter to specific edge kinds (repeatable)
  --transitive                    Follow edges transitively (BFS closure)
  --graph                         Print the full graph summary when no section is given
  --scc                           Run SCC analysis: list non-trivial cycles + lint
                                  warnings
  --json                          Emit JSON instead of human text
  --help                          Show this message and exit.
```

## `yuho repl`

Start interactive REPL for statute experimentation.

```
Usage: yuho repl [OPTIONS]

  Start interactive REPL for statute experimentation.

  The REPL provides an interactive environment for: - Parsing and validating Yuho code
  snippets - Transpiling to various targets (json, english, mermaid, etc.) - Exploring
  statute definitions and AST structure - Testing legal logic interactively

  Type 'help' within the REPL for available commands.

Options:
  --help  Show this message and exit.
```

## `yuho schema`

Print JSON Schema for Yuho JSON transpiler output.

```
Usage: yuho schema [OPTIONS]

  Print JSON Schema for Yuho JSON transpiler output.

  Useful for validating transpiler output or integrating with downstream tools that
  consume Yuho JSON.

  Examples:     yuho schema     yuho schema -o yuho-ast.schema.json

Options:
  -o, --output PATH  Output file path
  --help             Show this message and exit.
```

## `yuho serve`

Start the MCP (Model Context Protocol) server.

```
Usage: yuho serve [OPTIONS]

  Start the MCP (Model Context Protocol) server.

  Exposes Yuho functionality to AI assistants and editors.

Options:
  -p, --port INTEGER  Port to listen on (defaults to config mcp.port)
  --host TEXT         Host to bind to (defaults to config mcp.host)
  --stdio             Use stdio transport (for editor integration)
  --help              Show this message and exit.
```

## `yuho test`

Run tests for a Yuho statute file.

```
Usage: yuho test [OPTIONS] [FILE]

  Run tests for a Yuho statute file.

  Looks for test_<filename>.yh or tests/<filename>_test.yh

  Examples:     yuho test statute.yh     yuho test --all     yuho test --all --format
  junit

Options:
  --all                       Run all tests in directory
  --json                      Output results as JSON
  --coverage                  Enable coverage tracking
  --coverage-html PATH        Generate HTML coverage report
  --format [text|json|junit]  Output format
  --help                      Show this message and exit.
```

## `yuho transpile`

Transpile a Yuho source file to another format.

```
Usage: yuho transpile [OPTIONS] FILE

  Transpile a Yuho source file to another format.

  Supported targets: json, english, latex, pdf, mermaid, mindmap, alloy, docx,
  akomantoso. Use ``--shape schema`` with mermaid for the case-struct decision-tree
  rendering described in the 5-minutes guide.

Options:
  -t, --target [json|english|latex|pdf|mermaid|mindmap|mermaid-mindmap|svg|png|alloy|docx|akomantoso|akn|legaldocml]
                                  Transpilation target format
  -o, --output PATH               Output file path
  --dir PATH                      Output directory for multiple files
  --all                           Generate all targets
  --shape [statute|schema]        Mermaid flowchart shape: 'statute' (structural,
                                  default) or 'schema' (decision tree from a case-struct
                                  + matching fn). Only meaningful for the mermaid
                                  target.
  --json                          Output metadata as JSON
  --help                          Show this message and exit.
```

## `yuho verify`

Run formal verification checks (Alloy, Z3, or combined).

```
Usage: yuho verify [OPTIONS] [FILE]

  Run formal verification checks (Alloy, Z3, or combined).

  Use --capabilities to inspect available verifier backends and explicit unavailability
  reasons in your current environment.

Options:
  --engine [combined|alloy|z3]  Verification backend  [default: combined]
  --alloy-jar PATH              Path to Alloy analyzer JAR
  --alloy-timeout INTEGER       Alloy timeout in seconds  [default: 30]
  --z3-timeout-ms INTEGER       Z3 timeout in milliseconds  [default: 5000]
  --capabilities                Show verification backend availability and exit
  --json                        Output as JSON
  --help                        Show this message and exit.
```

## `yuho verify-report`

Generate a LaTeX verification report for statute completeness.

```
Usage: yuho verify-report [OPTIONS] FILE

  Generate a LaTeX verification report for statute completeness.

  Checks structural elements (title, actus reus, mens rea, penalty, definitions,
  illustrations, exceptions, jurisdiction, doc-comments) and produces a color-coded
  PASS/WARN/FAIL table.

  Examples:     yuho verify-report statute.yh     yuho verify-report statute.yh -o
  report.tex

Options:
  -o, --output PATH  Output .tex file path
  --help             Show this message and exit.
```

## `yuho wizard`

Interactive wizard for statute creation.

```
Usage: yuho wizard [OPTIONS]

  Interactive wizard for statute creation.

  Guides you through building a Yuho statute step-by-step using inquirer-style prompts.
  Generates valid .yh source files.

  Examples:     yuho wizard     yuho wizard -o my_statute.yh     yuho wizard --section
  299 --title "Culpable Homicide"

Options:
  -o, --output PATH  Output file path
  --section TEXT     Pre-set section number
  --title TEXT       Pre-set statute title
  --help             Show this message and exit.
```

---

## Global options

```
Usage: yuho [OPTIONS] COMMAND [ARGS]...

  Yuho - A domain-specific language for encoding legal statutes.

  Use 'yuho <command> --help' for command-specific help.

Options:
  --version             Show the version and exit.
  -v, --verbose         Enable verbose output
  --color / --no-color  Force color output on/off (auto-detected by default)
  -q, --quiet           Suppress non-error output
  --offline             Disable network operations and use local-only data paths where
                        supported
  --help                Show this message and exit.

Commands:
  ast                Visualize AST structure as tree.
  batch              Batch operations on directories of .yh files.
  check              Parse and validate a Yuho source file.
  ci-report          Run check+lint on all .yh files and produce a unified report.
  completion         Generate shell completion script.
  compliance-matrix  Generate a compliance checklist from statute elements.
  config             Manage Yuho configuration.
  contribute         Validate a statute file for contribution to the library.
  deps               Show statute dependencies for a Yuho file.
  diff               Compare two Yuho files and show semantic differences.
  eval               Evaluate a Yuho file through the interpreter.
  explain            Plain-language summary of an encoded statute.
  explore            Counter-example explorer: surface fact patterns over a section.
  fmt                Format a Yuho source file.
  generate           Generate statute scaffold with proper structure.
  generate-tests     Generate test cases using Z3 constraint solving.
  graph              Visualize statute dependencies as a graph.
  init               Initialize a new Yuho statute project.
  library            Manage Yuho statute packages.
  lint               Check Yuho files for style and best practice issues.
  lsp                Start the Language Server Protocol server.
  recommend          Charge-recommender: rank Penal Code sections that structurally...
  refs               Query the cross-section reference graph (G10).
  repl               Start interactive REPL for statute experimentation.
  schema             Print JSON Schema for Yuho JSON transpiler output.
  serve              Start the MCP (Model Context Protocol) server.
  test               Run tests for a Yuho statute file.
  transpile          Transpile a Yuho source file to another format.
  verify             Run formal verification checks (Alloy, Z3, or combined).
  verify-report      Generate a LaTeX verification report for statute completeness.
  wizard             Interactive wizard for statute creation.
```
