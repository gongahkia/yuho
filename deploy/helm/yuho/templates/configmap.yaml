apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-yuho-config
data:
  config.toml: |
    [mcp]
    host = "0.0.0.0"
    port = {{ .Values.service.port }}

    [transpile]
    default_target = "json"
