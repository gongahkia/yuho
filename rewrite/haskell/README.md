# Yuho Haskell production foundation

The [Singapore hurt-offence and section 84 research POC](../../docs/rewrite/SINGAPORE-HURT-SECTION84-HASKELL-POC.md) is an authored `.yh` model with synthetic scenarios and a deterministic Haskell `yuho explain` command. After `cabal v2-build exe:yuho --offline --jobs=1`, run `$(cabal list-bin exe:yuho) explain ../../research/singapore/section-84-pilot/hurt-offence/section323-with-section84.yh --scenario ../../research/singapore/section-84-pilot/hurt-offence/scenarios/06_nature_route.yh` from this directory. The result is a technical derivation, not a criminal-law conclusion.

The [multi-offence research POC](../../docs/rewrite/SINGAPORE-HURT-THEFT-SHARED-SECTION84-HASKELL-POC.md) explicitly attaches one authored s 84 definition to the hurt and theft candidates. For example, run `$(cabal list-bin exe:yuho) explain ../../research/singapore/section-84-pilot/multi-offence/section323-section379-with-section84.yh --scenario ../../research/singapore/section-84-pilot/multi-offence/scenarios/06_theft_nature.yh` from this directory. The scenario selects the theft candidate and supplies synthetic classifications and research-scope acknowledgements; the Haskell compiler instantiates only that candidate and its attached exception.

The [statutory-definitions POC](../../docs/rewrite/SINGAPORE-STATUTORY-DEFINITIONS-HASKELL-POC.md) authors `d:hurt`, `d:voluntarily-causing-hurt`, `d:wrongful-gain`, `d:wrongful-loss` and `d:dishonestly` once. For example, run `$(cabal list-bin exe:yuho) explain ../../research/singapore/section-84-pilot/statutory-definitions/section323-section379-definitions.yh --scenario ../../research/singapore/section-84-pilot/statutory-definitions/scenarios/10_theft_gain_intent.yh` from this directory. Executable `use` references contribute derived statuses; typed mental-state targets identify concepts without proving that the targeted result occurred. One shared section 84 graph remains explicitly attached to both candidates.

This workspace is the bounded production implementation under [accepted ADR-0001](../../docs/rewrite/ADR-0001-HASKELL.md) and [ADR-0002](../../docs/rewrite/ADR-0002-HASKELL-AUTHORITATIVE-FRONTEND.md). It includes the first [authoritative Haskell `yuho` frontend](../../docs/rewrite/HASKELL-YUHO-FRONTEND-POC.md) for the accepted section 84 and fictional v0.1 authored sources, plus the seven existing technical kernel fragments: `ClosedBooleanBranches-v1`, [AcyclicGuardedExceptions-v1](../../docs/rewrite/ACYCLIC-GUARDED-EXCEPTIONS-V1.md), [TypedBooleanFacts-v1](../../docs/rewrite/TYPED-BOOLEAN-FACTS-V1.md), [GuardedPenaltySelection-v1](../../docs/rewrite/GUARDED-PENALTY-SELECTION-V1.md), [PenaltyTerms-v1](../../docs/rewrite/PENALTY-TERMS-V1.md), [SuppliedProofStatus-v1](../../docs/rewrite/SUPPLIED-PROOF-STATUS-V1.md) and [RegisteredPresumptionDerivations-v1](../../docs/rewrite/REGISTERED-PRESUMPTION-DERIVATIONS-V1.md). The [language spike](../../experiments/language-spike/README.md) and its fixtures remain historical evidence. Python v0.1–v0.4 remains a frozen conformance oracle; Tree-sitter, legacy CLI/LSP, exporters and corpus tools are unchanged. The new `yuho` path does not invoke Python.

The one Cabal package keeps responsibilities in separate modules: `Yuho.Core.Types` and `Core.Source` own typed source identity and span checks; `Protocol.Json`, `Decode` and `Encode` own the bounded JSON boundary, closed request schema and canonical response; `Kernel.Validate`, `Evaluate` and `Run` own the original ordered Boolean semantics and structured rejection. `Yuho.Exception.Decode`, `Validate`, `Evaluate`, `Encode` and `Run` own the raw registry, resolved acyclic graph, pure exception semantics and fragment-specific result. `Yuho.TypedFacts` owns closed typed bindings, whole-registry annotation validation, Boolean projection and ordered observations while reusing the exception evaluator. `Yuho.PenaltySelection` owns closed declarations, whole-registry scope and occurrence validation, pure root-only selection, fragment-specific encoding and dispatch; it reuses the typed/exception evaluator without copying it. `Yuho.PenaltyTerms.Types`, `Decode`, `Validate`, `Encode` and `Run` own the closed source-ordered term algebra, complete-registry validation, selected-ID association and fifth result; they reuse the existing selector. `Yuho.SuppliedProofStatus` owns closed status assignments, generic structural validation, three-valued evaluation, trace-based penalty selection, sixth-only encoding and dispatch. `Yuho.Presumption` owns closed registrations and conditions, complete effective-leaf DAG validation, source-ordered route resolution, seventh-only encoding and dispatch. `app/Main.hs` owns line-oriented process I/O. `test/Main.hs` runs frozen-byte goldens, production hardening cases and semantic properties; `test/protocol.py`, `test/exception_protocol.py`, `test/typed_protocol.py`, `test/penalty_protocol.py`, `test/terms_protocol.py`, `test/proof_protocol.py` and `test/presumption_protocol.py` exercise persistent executables. The [production request](schema/request.schema.json) and [result](schema/result.schema.json) schemas discriminate all seven fragments without changing the frozen spike schemas.

## KernelInput/Result v1 decisions

- One UTF-8 JSON request per line yields one canonical UTF-8 JSON response per line. No log text goes to stdout. A request can be at most **1,048,576 bytes excluding the line terminator**; the stream reader drains an overlong line and returns `KDEC002` before processing the next request. JSON container nesting is capped at **64** and `policy.max_nodes` must be **1–1,024**. IDs are counted in the parsed token tree before recursive domain construction; all semantic nodes are checked again after construction. The fourth fragment additionally caps expanded branch/declaration occurrences across the entire registry at **4,096** before selection. The fifth counts every term node in `max_nodes` and caps term depth at **16**. The seventh counts every registration and condition node and caps condition depth at **16**.
- All fixed-shape protocol objects have closed fields, including nested source, policy, parser result, diagnostic, provision, requirement, exception, guard, penalty declaration, term, endpoint, proof status, assignment source and span objects; facts and diagnostic parameters are typed dynamic-key maps. Duplicate JSON object keys, including differently escaped equal keys, fail decoding. `KPROT001` distinguishes protocol version; `KDEC001/002` cover JSON/shape/resource decoding; `KINV001–009` cover missing facts, duplicate IDs, spans, graph references/cycles, typed binding mismatch, penalty-guard scope and term invariants; `KINV010` covers status-assignment identity/source/span invariants; `KCAP001` covers unsupported fragments, constructors, currencies, sentencing features, terms or deferred presumption features; `KERR001` denotes evaluation inconsistency. Every rejected request has `status=rejected` and no legal rule result; invalid input cannot be interpreted as a negative legal judgment.
- The production fragment **keeps `definitions`**. A provision with `definitions=true` cannot also contain requirements or children. A program with no executable branch must contain an explicitly marked definition provision; it then yields `definition_only` and `status=false`, preserving the frozen semantic contract without accepting an unmarked empty program. A leaf must omit `members`; each `all`/`any` group must have at least one member. Facts are total over leaf IDs and cannot introduce extra names. Semantic IDs are unique across provision and requirement nodes.
- `reference_date` is exactly `YYYY-MM-DD` and must exist in the Gregorian calendar. Parser `accepted=true` cannot coexist with an error-severity diagnostic. An unsupported source version or operation is rejected; the parser diagnostic transport remains a transport fixture, not a new parser.
- Source text is retained as `Text` for identity and as UTF-8 `ByteString` for hashing and spans. Offsets are zero-based, half-open UTF-8 byte offsets; displayed lines and columns are **one-based byte columns**, consistent with the current Tree-sitter convention. Both ends must be UTF-8 code-point boundaries, lie within the source byte length, and agree with the displayed line/column calculated by counting LF bytes. Parent spans contain child spans. This is a v1 policy; changing the editor-facing display convention requires a versioned contract.
- Canonical output explicitly sorts mapping keys and uses compact separators while retaining declaration order in arrays and trace edges. Aeson supplies maintained string/Unicode parsing and string escaping; `Protocol.Json` uses its exposed token stream so duplicate keys are visible before a map can overwrite them. Crypton supplies SHA-256. Properly paired surrogate escapes and non-BMP Unicode are accepted; malformed surrogates are rejected. Canonical IR v1.2 and its hash rules are untouched.

The result is a typed `Either` validation/evaluation flow, with ADTs for result status and provision kind. The exception fragment validates the whole registry, resolves qualified targets to rule keys and rejects cycles before evaluation. It shares one total fact map, reference date and policy across all targets. It evaluates every guard attached to an ordinarily satisfied branch, reports every true exception, keeps declaration order only in traces and distinguishes defeat from failed ordinary requirements. The typed fragment requires closed `{type:bool,value:Boolean}` records keyed by globally unique executable leaf IDs, checks any declared burden/standard across the full registry before evaluation, and projects `value` once for that same exception evaluator. Incompatible annotations reject as `KINV007` with no rule judgment. A matching annotation is not proof; optional provenance is descriptive trace data, and `source_label` is not an authenticated authority or stable registry reference. The fourth fragment validates globally unique `pen:` IDs, source spans, exact same-path guard scope and inherited occurrences before selecting declaration IDs from final-true **root** branches. It reads guard truth from existing leaf traces, aggregates every candidate, deduplicates each selected ID with ordered supports, and emits non-fatal path-specific `KSEL001` warnings. The fifth validates every source-contained term tree before evaluation and associates the canonical tree with each selected ID only. The sixth accepts external statuses rather than Boolean facts, checks every assignment and declared annotation, and traces a separate three-valued satisfaction projection through the same acyclic graph. `not_proved` is not factual falsity; S/N/U is not conviction/acquittal. Neither selected declarations nor term combinators represent a sentence. The seventh computes effective leaf satisfaction from all registered rebuttable technical routes without changing supplied classifications. It validates every condition reference and rejects cycles before rule evaluation, preserves complete route traces, and does not allocate burdens or claim legal presumptions. The [proof-neutral vectors](test/presumption-fixtures/PROOF-VECTORS.json) do not select a proof tool. These tests establish bounded protocol and technical semantics, not legal fidelity or a proof of correspondence.

| Capability | Kernel status | Boundary |
|---|---|---|
| `ClosedBooleanBranches-v1` | Implemented; 15 frozen responses remain exact | Total Boolean facts, recursive named groups, inherited branches. |
| `AcyclicGuardedExceptions-v1` | Implemented and synthetic-tested | Closed acyclic registry, same context, simple registered guards and branch defeat only. Valid wire requests currently produce Boolean judgments; unresolved aggregation is tested internally. |
| `TypedBooleanFacts-v1` | Implemented and synthetic-tested | Closed Boolean leaf bindings, exact IDs, optional burden/standard annotations with `KINV007` mismatch rejection, descriptive provenance and same-context registered targets. No proof state. |
| `GuardedPenaltySelection-v1` | Implemented and synthetic-tested | Selects declaration IDs/supporting root branch paths only, with exact evaluated-leaf guards, `KINV008` scope rejection and path-specific `KSEL001` overlap warning. No punishment term or sentence. |
| `PenaltyTerms-v1` | Implemented and synthetic-tested | Closed authored term trees for five atom families, explicit bounds, full-registry validation and canonical terms on selected IDs only. No sentencing or proof state. |
| `SuppliedProofStatus-v1` | Implemented and synthetic-tested | External proved/not-proved/reasoned-unresolved assignments, source-checked observations, separate S/N/U projection and U-aware exception/penalty traces. No evidence assessment or outcome. |
| `RegisteredPresumptionDerivations-v1` | Implemented and synthetic-tested | Source-linked, acyclic effective-leaf technical routes with separate direct/effective observations; no legal authority, burden shift or outcome. |
| Doctrinal presumptions, outcomes, CPC and case-law effects | Unsupported | Require later versioned fragments and independent review. |
| Haskell source frontend and native `yuho` CLI | Bounded research POC implemented | Standalone section 84, fictional offence-plus-exception, and synthetic section 323 candidate with section 84; no LSP, corpus migration or formal proof. |

## Separate ModelBundle-v1 package validator

`yuho-model-bundle validate <bundle-directory>` implements the accepted [non-executable package contract](../../docs/rewrite/MODEL-BUNDLE-V1.md). It checks exact artifact bytes, source descriptions, semantic mappings, positive scope and detached unsigned reviews offline. It does not add an eighth kernel variant, assess legal correctness, authenticate a reviewer or change existing kernel results. `Yuho.ModelBundle.Types`, `Json`, `Validate`, `Package`, `Review` and `Run` keep this build/package boundary separate from rule evaluation. The [CLI fixture suite](test/model_bundle_protocol.py) and [schemas](schema/README.md) are independent of the existing B/H/E/T/GP/PT/PS/RD fixtures.

`yuho-model-bundle diff <old-bundle-directory> <new-bundle-directory>` emits a closed [ModelBundleChangeSet-v1](../../docs/rewrite/MODEL-BUNDLE-CHANGE-IMPACT-PROVISIONAL-SPEC.md) mechanical report after validating both immutable v1 packages. A changed core digest makes an old review assertion non-applicable to the new core; no review is transferred. `ChangeSet` and `Diff` add no kernel variant, legal-update inference, source fetching or deployment decision. Success writes one canonical JSON line to stdout. Diagnostics go to stderr; exits are 0 for a complete comparison, 1 for invalid/incomparable bundles, 2 for usage/I/O, and 4 for comparator output limits. Exit 3 remains reserved for `validate` review policy. [Committed comparison snapshots](test/model-bundle-diff-fixtures/CASES.json) are updated only with an explicit `--update-snapshots` invocation after review.

## Toolchain and dependencies

`cabal.project` selects **GHC 9.8.4** and serial builds. The project index state and `cabal.project.freeze` pin the complete resolved dependency graph; `yuho-foundation.cabal` pins each direct dependency exactly. `-Wall -Werror -O1` applies to library, executable and tests; no warning suppression is used. The new CLI also uses `ScopedTypeVariables` for explicit I/O exception handling.

| Dependency | Why it is here |
|---|---|
| `base` | Standard ADTs, `Either`, traversal and subprocess I/O. |
| `aeson` | Maintained JSON tokenizer, Unicode and string escaping; canonical object order remains explicit in our encoder. |
| `bytestring` | Bounded wire bytes, UTF-8 source bytes and output builders. |
| `containers` | Deterministic fact maps, duplicate-key and duplicate-ID sets. |
| `crypton` | Maintained SHA-256 implementation for source and input digests. |
| `text` | Decoded source, IDs, paths and diagnostic fields. |
| `time` | Real Gregorian calendar-date validation. |
| `QuickCheck` (tests) | Semantic invariants over randomized fact values and JSON key order. |
| `directory`, `filepath` | Check and traverse the closed unpacked bundle profile; locate test fixtures. |
| `unix` | Inspect filesystem object types without following symlinks, so artifact/review entries cannot be accepted as ordinary files through a symlink or special node. |

Transitive packages in the freeze file are pinned because they are required by these direct libraries; they carry no Yuho semantics. The exposed Aeson token API is a deliberate upgrade-review point: changing Aeson requires rerunning duplicate-key, Unicode, depth and canonical-byte tests.

## Reproduce

From this directory, with the pinned GHC/Cabal available:

```sh
cabal v2-build exe:yuho-kernel --jobs=1
cabal v2-build exe:yuho-model-bundle --offline --jobs=1
cabal v2-test foundation-test --jobs=1 --test-show-details=direct
python test/protocol.py "$(cabal list-bin exe:yuho-kernel)"
python test/exception_protocol.py "$(cabal list-bin exe:yuho-kernel)"
python test/typed_protocol.py "$(cabal list-bin exe:yuho-kernel)"
python test/penalty_protocol.py "$(cabal list-bin exe:yuho-kernel)"
python test/terms_protocol.py "$(cabal list-bin exe:yuho-kernel)"
python test/proof_protocol.py "$(cabal list-bin exe:yuho-kernel)"
python test/presumption_protocol.py "$(cabal list-bin exe:yuho-kernel)"
python test/prior_bytes.py
python test/model_bundle_protocol.py "$(cabal list-bin exe:yuho-model-bundle)"
python test/model_bundle_diff_protocol.py "$(cabal list-bin exe:yuho-model-bundle)"
```

The [hardening cases](test/fixtures/CASES.json) are H01–H21; `test/fixtures/generate.py` documents how they derive from the frozen source inputs. H12 is over the request-byte limit and is checked at the subprocess boundary. The [exception cases](test/exception-fixtures/CASES.json) are E01–E40 with a SHA-256 [manifest](test/exception-fixtures/MANIFEST.json). The [typed cases](test/typed-fixtures/CASES.json) are T01–T53 with separate [manifest](test/typed-fixtures/MANIFEST.json), [migration probes](test/typed-fixtures/MIGRATION-PROBES.json) and [vectors](test/typed-fixtures/PROOF-VECTORS.json). The [penalty cases](test/penalty-fixtures/CASES.json) are GP01–GP49 with separate [manifest](test/penalty-fixtures/MANIFEST.json), [migration adapter](test/penalty-fixtures/legacy_adapter.py) and [vectors](test/penalty-fixtures/PROOF-VECTORS.json). The [term cases](test/term-fixtures/CASES.json) are PT01–PT68 with separate [manifest](test/term-fixtures/MANIFEST.json), [migration probes](test/term-fixtures/MIGRATION-PROBES.json) and [vectors](test/term-fixtures/PROOF-VECTORS.json). The [supplied-status cases](test/proof-fixtures/CASES.json) are PS01–PS72 with a separate [manifest](test/proof-fixtures/MANIFEST.json) and [vectors](test/proof-fixtures/PROOF-VECTORS.json). The [presumption cases](test/presumption-fixtures/CASES.json) are RD01–RD77 with a separate [manifest](test/presumption-fixtures/MANIFEST.json) and [proof-neutral vectors](test/presumption-fixtures/PROOF-VECTORS.json). PT, PS and RD fixture filenames use two-letter prefixes; their wire request IDs remain three characters. The production tests still compare every frozen spike response byte for byte. See [test evidence](TEST-RESULTS.md) for the checked host and commands.

Before any doctrine-specific exception priority or defeat relation, a new versioned fragment and legal review are required. Jurisdiction-reviewed presumptions, evidence assessment, sentencing and outcomes are not implemented here. Native parser, LSP, proof correspondence, authenticated fact provenance and cross-platform packaging gates remain separate from this kernel result.
