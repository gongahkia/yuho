# Haskell foundation targeted verification

## RegisteredPresumptionDerivations-v1 completion

On 13 September 2026 the seventh closed fragment was checked with pinned GHC 9.8.4/Cabal, serial offline builds and the existing `-Wall -Werror -O1` policy. No Haskell dependency, compiler extension or warning suppression was added. The [RD cases](test/presumption-fixtures/CASES.json) and [proof-neutral vectors](test/presumption-fixtures/PROOF-VECTORS.json) are synthetic technical evidence, not Singapore doctrine or a formal correspondence proof.

| Check | Result |
|---|---|
| `cabal v2-build all --offline --jobs=1` | Pass under `-Wall -Werror -O1`. |
| `cabal v2-test test:foundation-test --offline --jobs=1 --test-show-details=never` | Pass: 15 frozen Boolean exact-byte goldens; H01–H21, E01–E40, T01–T53, GP01–GP49, PT01–PT68, PS01–PS72; 77 RD cases (one overlong stream case is checked by the protocol harness); all earlier bounded properties plus 14 new RD properties. |
| `python test/{protocol,exception_protocol,typed_protocol,penalty_protocol,terms_protocol,proof_protocol,presumption_protocol}.py "$(cabal list-bin exe:yuho-kernel)"` (run serially as individual scripts) | Pass: previous persistent suites and 77 RD fixtures in 112 persistent requests; accepted request and all result schemas, proof-neutral vectors, ordered complete traces, canonical bytes, manifest SHA-256, isolated byte-identical regeneration, duplicate-key/Unicode/malformed-input cases and rejection recovery. |
| `python test/prior_bytes.py` | Pass: a temporary `git archive 9dd40b6` offline baseline executable and current executable produce **318 identical prior response byte strings** across 15 frozen, 21 H, 40 E, 53 T, 49 GP, 68 PT and 72 PS cases, including all accepted earlier requests. The serial archive build was removed afterward. |
| `python test/offline_replay.py` | Pass: clean temporary-directory pinned offline Cabal build and local install, then representative prior launches and RD01/RD08/RD36/RD46/RD59/RD73 accepted, chained, missing-reference, cyclic and oversized launches. |
| `python test/docs.py`; `python ../../scripts/verify_capability_claims.py`; `git diff --check` | Pass: eleven structured active documents, 209 relative links, bounded public claims and whitespace. |

Immediately before the final clean replay, `free -h` reported **15 GiB total, 5.4 GiB available, and 7.2/8.0 GiB swap used**. The largest observed resident processes were Helium (~713 MiB), Deadcells (~642 MiB) and Baloo (~627 MiB). The serial replay completed, but no comparable latency or peak-RSS benchmark was attempted; elapsed command times are not benchmark evidence. The full Python suite, corpus exports, parser regeneration, Lean, Z3 and Docker were intentionally not run. Source hashes are not authenticated authority, and passing tests do not establish evidence sufficiency, doctrinal validity, legal outcomes or formal refinement.

## Original foundation verification

**Host:** Fedora workstation, 12 September 2026; GHC 9.8.4, Cabal 3.10.3.0, Python 3.14.7 from `/usr/bin/python`. Commands below ran serially in `rewrite/haskell/` unless stated otherwise. This is a foundation check, not a new Haskell-versus-OCaml benchmark.

| Check | Result |
|---|---|
| `cabal v2-build exe:yuho-kernel --jobs=1` | Pass with `-Wall -Werror -O1`; no warning suppressions. |
| `cabal v2-test foundation-test --jobs=1 --test-show-details=direct` | Pass: 15 frozen exact-byte goldens, 20 in-process hardening cases (H12 belongs to the stream reader), definition-only unit check, seven QuickCheck properties × 100 cases. |
| `python test/protocol.py "$(cabal list-bin exe:yuho-kernel)"` | Pass: all 15 frozen responses match byte for byte; H01–H21 expected status/code/stage; one 1,048,578-byte line rejected and drained; 20 following valid requests and a repeated invalid request match their earlier bytes; stdout has one response per request and stderr is empty. H16 digest independently matches Python's canonical Unicode JSON/SHA-256. |
| Frozen result-v1 JSON Schema validation with installed `jsonschema` | Pass: 36 responses (15 frozen, 21 hardening) conform. The validator emitted only its `RefResolver` deprecation warning. |
| Clean temporary-directory `cabal v2-build exe:yuho-kernel --offline --jobs=1` | Pass using the pinned freeze file and existing user cache; no system-wide install. |
| Source scan for partial semantic operations | Pass: no `head`, `tail`, `fromJust`, `undefined`, `!!`, unchecked map lookup, semantic `error` or unsafe I/O. The bounded UTF-8 byte indexing in `Core.Source` has an explicit range guard. |
| `python scripts/verify_capability_claims.py`, active-document relative links, `git diff --check` | Pass: capability boundaries, 121 relative links in ten active documents, and whitespace. The claim checker initially matched a negated formal-assurance phrase in the ADR; wording was clarified and the rerun passed. |

The hardening manifest lists each production-only case and expected diagnostic. H01/H02/H18 check closed fields, H03–H05 branch shape, H06 real dates, H07 parser acceptance, H08/H21 both definition-state directions, H09/H10/H19 source byte/display spans, H11/H12/H20 resource limits, H13 duplicate IDs, H14 unsupported capability, H15 duplicate escaped JSON key, and H16/H17 valid/invalid Unicode surrogate handling. B01–B07, R01–R04 and P01–P04 were neither edited nor reinterpreted.

Before the single resource sample, `free -h` reported **15 GiB total, 8.3 GiB available and 4.5/8.0 GiB swap used**. The largest visible resident processes were Baloo (~675 MiB) and Helium (~611 MiB). A single serial `/usr/bin/time` launch of B06 reported **8,644 KiB peak RSS** and elapsed `0.00 s` at the tool's hundredth-second resolution. This isolated sample is not a median, latency gate or whole-toolchain forecast. The pinned dependency build was also serial; no concurrent candidate run occurred.

Only targeted checks were run. The full Python suite, full corpus exports, grammar regeneration, Lean/Lake, Docker, new parser/LSP, cross-platform packaging, proof correspondence, and the next semantic fragment were intentionally not run. Host-specific parser/LSP and packaging risk gates in the [ADR](../../docs/rewrite/ADR-0001-HASKELL.md) remain open.

## AcyclicGuardedExceptions-v1 completion

The separately [specified exception fragment](../../docs/rewrite/ACYCLIC-GUARDED-EXCEPTIONS-V1.md) was checked on the same Fedora host with the pinned GHC 9.8.4/Cabal workspace, serially and offline. It adds no direct or transitive Haskell dependency, warning suppression or compiler extension. The public release capability registry remains unchanged because this subprocess is not yet a product CLI/LSP capability; the [foundation capability table](README.md) describes its actual scope.

| Check | Result |
|---|---|
| `cabal v2-build exe:yuho-kernel --offline --jobs=1` | Pass under `-Wall -Werror -O1`. |
| `cabal v2-test foundation-test --offline --jobs=1 --test-show-details=direct` | Pass: all 15 frozen exact-byte responses, H01–H21, 39 in-process E fixtures (E35 is a stream-limit case), the existing seven properties and eleven exception properties ×100 generated cases, plus pure unresolved truth-table checks. |
| `python test/protocol.py "$(cabal list-bin exe:yuho-kernel)"` | Pass: 15 frozen goldens and 21 H hardening cases with persistent recovery. |
| `python test/exception_protocol.py "$(cabal list-bin exe:yuho-kernel)"` | Pass: 40 E fixtures, 82 persistent requests, recovery after every rejected case, all result schemas, 13 valid request schemas, canonical repeated bytes, digest, branch/dependency vectors and no stdout/stderr contamination. E32/E33/E34 exercise duplicate escaped keys and malformed/valid non-BMP Unicode; E35 exercises overlong-line draining. |
| Production union schema replay | Pass: nine accepted Boolean requests and all 36 Boolean responses validate. An initial overly broad one-off command tried to validate R03 as a valid request; R03 deliberately has an unsupported constructor. Restricting request validation to accepted inputs corrected that test assertion; all result variants were still validated. |
| Exception generator and manifest | Pass: 44 files are byte-identical after regeneration; the subprocess harness checks every SHA-256 manifest entry. E39 adds capability-class rejection of an unsupported guard that also carries extra fields; E40 checks duplicate executable-branch IDs. |
| Clean temporary-directory offline build and local copy install | Pass: serial `cabal v2-build` and `cabal v2-install` with the pinned freeze and existing user cache; launching the installed executable on E02 produced the expected defeated-branch result. No system-wide install or network fetch was used. |
| `python scripts/verify_capability_claims.py` and active rewrite relative links | Pass: the public capability/evidence boundary remains valid and 106 active relative links resolve. |

Before the clean offline replay, `free -h` reported **15 GiB total, about 6.4 GiB available and 5.0/8.0 GiB swap used**. The largest visible resident process was Baloo (~855 MiB), followed by several Helium processes (~270–467 MiB each). The build/install replay was run serially and completed; no precise latency or peak-RSS benchmark was attempted. The full Python suite, corpus exports, parser regeneration, Lean/Lake, Docker, native parser/LSP work, proof-tool selection and later semantic fragments remain intentionally unrun or unimplemented. These tests do not establish doctrine-level fidelity, formal refinement or cross-platform packaging parity.

## TypedBooleanFacts-v1 completion

The [third technical fragment](../../docs/rewrite/TYPED-BOOLEAN-FACTS-V1.md) adds no dependency, compiler extension or warning suppression. Typed bindings are validated across the complete acyclic registry before their Boolean projection enters the unchanged exception evaluator. The production schema is now a three-way closed discriminated union; the previous two response variants remain exact. Public product capability claims remain unchanged because this subprocess is not yet wired into the CLI or LSP.

| Check | Result |
|---|---|
| `cabal v2-build exe:yuho-kernel --jobs=1 --offline` | Pass with pinned GHC 9.8.4 and `-Wall -Werror -O1`. |
| `cabal v2-test foundation-test --jobs=1 --offline --test-show-details=direct` | Pass: 15 frozen exact-byte goldens; H01–H21 (H12 at stream boundary); E01–E40 (E35 at stream boundary); 52 in-process T fixtures (T49 at stream boundary); seven existing Boolean properties ×100, eleven exception properties ×100, and fifteen new bounded typed properties ×25. |
| `python3 test/protocol.py "$(cabal list-bin exe:yuho-kernel)"` | Pass: all 15 frozen bytes, H01–H21, persistent recovery and canonical schema. |
| `python3 test/exception_protocol.py "$(cabal list-bin exe:yuho-kernel)"` | Pass: all 40 E cases, 82 persistent requests, schemas, vectors, deterministic bytes and recovery. |
| `python3 test/typed_protocol.py "$(cabal list-bin exe:yuho-kernel)"` | Pass: 53 T cases (21 accepted, 32 rejected), 100 persistent requests, recovery after each rejection, 21 accepted request schemas, all 53 result schemas, 53 proof-neutral vectors, exact `KINV007` parameters/spans, digest and canonical-byte checks. T25/T44/T48 cover duplicate escaped keys, valid non-BMP text and malformed surrogate; T49 covers oversized-line draining, and T53 checks distinct similar leaf IDs. Five migration probes exercise explicit legacy projection dispositions. |
| Typed generator and manifest integrity | Pass: 59 files byte-identical after regeneration; the protocol harness verifies all manifest SHA-256 entries. |
| `PYTHONDONTWRITEBYTECODE=1 YUHO_ANALYSIS_CACHE=0 .venv/bin/python -m pytest tests/test_burden_runtime.py -q -p no:cacheprovider` | Pass: three historical Python migration observations, deliberately not treated as typed-kernel parity requirements. |
| `python3 test/docs.py`; `python3 scripts/verify_capability_claims.py`; `git diff --check` | Pass: seven active documents structured, 115 relative links resolve, public capability boundaries remain valid, and whitespace is clean. |
| `python3 test/offline_replay.py` | Pass: clean temporary-directory offline Cabal build/install using the pinned cache, then installed-executable T17 defeat and T49 resource rejection. The first replay attempt built but its sdist install failed because the replay script had omitted the workspace test directory; the copy set was corrected and the full replay passed. |

Before the final successful clean replay, `free -h` reported **15 GiB total, 5.7 GiB available and 5.2/8.0 GiB swap used**. The largest visible resident processes were Deadcells (~1.16 GiB), Baloo (~997 MiB) and Helium (~564 MiB). All builds and subprocess suites ran serially. No precise latency or peak-RSS benchmark was attempted; command elapsed output is not a benchmark. The full Python suite, corpus exports, grammar regeneration, Lean/Lake, Docker, parser/LSP migration, proof states, authenticated provenance, guarded penalties and cross-platform package checks were intentionally not run. The fragment has validation, fixtures and properties, not formal refinement or legal review.

## GuardedPenaltySelection-v1 completion

The [fourth technical fragment](../../docs/rewrite/GUARDED-PENALTY-SELECTION-V1.md) adds closed declaration IDs and exact trace-based leaf guards, whole-registry scope validation, a 4,096 expanded-occurrence cap, root-only candidate selection and path-specific non-fatal `KSEL001`. It adds no Haskell dependency, warning suppression or advanced compiler extension. Its [GP fixtures](test/penalty-fixtures/CASES.json) and [proof-neutral vectors](test/penalty-fixtures/PROOF-VECTORS.json) are synthetic technical evidence, not sentencing or legal review. The public Python CLI/LSP capability registry remains unchanged.

| Check | Result |
|---|---|
| `cabal v2-build all --jobs=1 --offline` | Pass with pinned GHC 9.8.4 and `-Wall -Werror -O1`. |
| `cabal v2-test foundation-test --jobs=1 --offline --test-show-details=direct` | Pass: 15 frozen Boolean response bytes, H01–H21 (H12 at stream boundary), E01–E40 (E35 at stream boundary), T01–T53 (T49 at stream boundary), all 49 GP cases, seven Boolean and eleven exception properties ×100, fifteen typed and eighteen GP properties ×25, and three GP pure/internal cases including unresolved skip and disjoint-path overlap. GP38's 4,096-result boundary is decoded in the test with Aeson because the request-only `Protocol.Json` decoder intentionally caps bytes at 1 MiB. |
| `python3 test/protocol.py "$(cabal list-bin exe:yuho-kernel --offline)"` | Pass: all 15 frozen goldens, 21 hardening cases, canonical bytes and persistent recovery. |
| `python3 test/exception_protocol.py "$(cabal list-bin exe:yuho-kernel --offline)"` | Pass: 40 E fixtures, 82 persistent requests, schemas, vectors, deterministic bytes and recovery. |
| `python3 test/typed_protocol.py "$(cabal list-bin exe:yuho-kernel --offline)"` | Pass: 53 T fixtures, 100 persistent requests, schemas, vectors, bytes and recovery. |
| `python3 test/penalty_protocol.py "$(cabal list-bin exe:yuho-kernel --offline)"` | Pass: 49 GP fixtures, 76 persistent requests, all accepted request and every result schema, manifest SHA-256, isolated byte-identical fixture regeneration, proof-neutral vectors, input digests, canonical response bytes, all rejection recovery and explicit migration-adapter notices/rejections. GP38 accepts exactly 4,096 expanded occurrences; GP39 rejects 4,160 with `KDEC002`. GP34 exercises duplicate JSON keys; GP44 carries non-BMP Unicode. |
| `python3 test/docs.py`; `python3 scripts/verify_capability_claims.py`; `git diff --check` | Pass: eight active documents structured, 140 relative links resolved, public capability/evidence claims bounded, whitespace clean. |
| `python3 test/offline_replay.py` | Pass: serial clean temporary-directory pinned offline build and Cabal local install; installed launches of T17/T49 and GP13/GP39 gave expected results. No network dependency, global install or cross-platform claim. |

Immediately before the clean replay against committed code, `free -h` reported **15 GiB total, 7.8 GiB available and 4.9/8.0 GiB swap used**; the largest visible resident processes were Baloo (~890 MiB), Handy (~788 MiB) and Helium (~632 MiB). The replay was serial and resource-safe on that observed host. No latency distribution or peak-RSS benchmark was run, and coarse command elapsed time is not a benchmark. The full Python suite, corpus exports, parser regeneration, Lean, Z3, Docker, production CLI/LSP migration, proof statuses, `PenaltyTerms-v1`, criminal outcomes and section 84/CPC work were intentionally not run or implemented. The fourth fragment has typed validation, synthetic fixtures, bounded properties and proof-neutral vectors, not a theorem, formal refinement or legal-correctness claim.

## PenaltyTerms-v1 completion

The [fifth technical fragment](../../docs/rewrite/PENALTY-TERMS-V1.md) adds one closed, source-ordered term tree per penalty declaration. It validates every term in the complete registry before evaluation and adds a canonical term only to selected records. It changes no fourth-fragment selection rule, proof status or sentence. No new Haskell dependency, compiler extension or warning suppression was added; the pinned GHC 9.8.4/Cabal workspace remains under `-Wall -Werror -O1`.

| Check | Result |
|---|---|
| `cabal build --offline all` | Pass, serial, using the pinned compiler and `-Wall -Werror`. |
| `cabal test --offline foundation-test --test-show-details=never` | Pass: all 15 frozen Boolean exact-byte goldens, H01–H21, E01–E40, T01–T53, GP01–GP49 and PT01–PT68. Existing seven Boolean and eleven exception properties ran ×100; fifteen typed, eighteen GP and eighteen PT bounded properties ran ×25, plus fragment-specific direct/internal checks. PT66 is checked in-process using the existing generic byte-limit `KDEC001` and through the stream reader as `KDEC002`. |
| `python test/protocol.py "$(cabal list-bin --offline exe:yuho-kernel)"` | Pass: frozen goldens, hardening and persistent recovery. |
| `python test/exception_protocol.py "$(cabal list-bin --offline exe:yuho-kernel)"` | Pass: 40 E fixtures, 82 persistent requests, schema, vectors, canonical bytes and recovery. |
| `python test/typed_protocol.py "$(cabal list-bin --offline exe:yuho-kernel)"` | Pass: 53 T fixtures, 100 persistent requests, schemas, vectors, bytes and recovery. |
| `python test/penalty_protocol.py "$(cabal list-bin --offline exe:yuho-kernel)"` | Pass: 49 GP fixtures, 76 persistent requests, schemas, vectors, bytes and recovery; earlier selection fields remain stable. |
| `python test/terms_protocol.py "$(cabal list-bin --offline exe:yuho-kernel)"` | Pass: 68 PT fixtures (30 accepted, 38 rejected), 110 persistent requests, all accepted request and all result schemas, complete proof-neutral vector checks, exact canonical bytes, SHA-256 manifest, isolated byte-identical fixture regeneration, rejection recovery after every rejected case and per-accepted-request equality of fourth-fragment rules, selected IDs/supports, selection traces and warnings after stripping terms. PT65/PT68 reject duplicate keys, PT59 transports non-BMP Unicode, PT67 rejects a malformed surrogate, and PT66 exercises oversized-line draining/recovery. |
| `python rewrite/haskell/test/docs.py`; `python scripts/verify_capability_claims.py`; `git diff --check` (repository root) | Pass: nine active structured documents and 168 relative links, bounded public capability claims, and whitespace. |
| `python test/offline_replay.py` | Pass: serial clean temporary-directory offline Cabal build and local install using the pinned cache; installed launches of T17/T49, GP13/GP39 and PT03/PT66 returned the expected result/rejection, including canonical `"1.20"` and resource recovery. |

Before the first clean replay, `free -h` reported **15 GiB total, 7.9 GiB available and 4.9/8.0 GiB swap used**. Before the final source-byte replay it reported **7.4 GiB available** with the same swap use. The largest visible resident processes at that final check were Handy (~893 MiB), Baloo (~815 MiB) and Helium (~755 MiB). The replays ran serially; no latency distribution or peak-RSS benchmark was attempted, and the commands' coarse elapsed times are not presented as benchmarks. The full Python suite, corpus exports, parser regeneration, Lean, Z3, Docker, proof statuses, sentencing, criminal outcomes and section 84/CPC work were intentionally not run. Fixtures and properties establish bounded technical behavior only; they do not prove formal correspondence or legal fidelity.

## SuppliedProofStatus-v1 completion

The [sixth technical fragment](../../docs/rewrite/SUPPLIED-PROOF-STATUS-V1.md) validates externally assigned status/source records across the complete registry, applies a separate three-valued satisfaction projection, evaluates every requirement and applicable exception in declaration order, and selects penalties only from final-satisfied root branches. The `guard_unresolved` skip is reachable without treating malformed input as uncertainty. `KINV010` covers assignment identity and source/span invariants. Prior Boolean/exception/typed/selection/term paths remain independently dispatched. No dependency, warning suppression or advanced Haskell extension was added; the pinned GHC 9.8.4 workspace retains `-Wall -Werror -O1`.

| Check | Result |
|---|---|
| `cabal build --offline all` | Pass, serial, under pinned GHC 9.8.4 and `-Wall -Werror`. |
| `cabal test --offline foundation-test --test-show-details=never` | Pass: 15 exact frozen Boolean goldens; H01–H21, E01–E40, T01–T53, GP01–GP49, PT01–PT68 and PS01–PS72; all prior properties plus 20 bounded PS properties (30 examples each), 18 direct pairwise truth-table entries and six PS direct checks. PS65 is the oversized in-process byte path (`KDEC001`); the stream reader tests `KDEC002`. |
| `python3 test/protocol.py` through `python3 test/terms_protocol.py` against the built executable | Pass: respectively 15 frozen/21 hardening, 40 E/82 persistent, 53 T/100 persistent, 49 GP/76 persistent and 68 PT/110 persistent cases, including previous schema, vector, duplicate-key, Unicode, malformed-input, canonical-byte and recovery checks. |
| `python3 test/proof_protocol.py <executable>` | Pass: 72 PS cases (38 accepted, 34 rejected), 111 requests in one process, accepted sixth-request and every response schema, independent structural/projection vectors, source and trace order, input digests, canonical exact bytes, SHA-256 manifest, isolated byte-identical regeneration, and recovery after every rejection. PS31 retains an unresolved exception trace when a satisfied guard defeats; PS33 skips an unresolved penalty guard on an otherwise satisfied Any branch. PS63/PS66 cover duplicate keys and malformed surrogate; PS57 carries non-BMP text; PS65 covers oversized-line draining. |
| Previous-fragment baseline comparison | Pass: a clean temporary offline build from `git archive HEAD` at `9996f3703e37cb299fa26c119ad42ec3b221607e` was compared byte for byte with the sixth-fragment executable on 246 frozen B/P/R, H, E, T, GP and PT requests, including rejections. Zero response mismatches. The first five schema variant objects were kept unchanged. |
| `python3 test/docs.py`; `python3 scripts/verify_capability_claims.py`; `git diff --check` | Pass: ten structured active documents and 189 relative links, bounded public capability claims, and whitespace. |
| `python3 test/offline_replay.py` | Pass: serial clean temporary-directory offline Cabal build and local binary install from the pinned cache, followed by installed T17/T49, GP13/GP39, PT03/PT66 and PS31/PS33/PS45/PS65 launches. The installed PS33 trace reports `guard_unresolved`; PS45 is `KINV010`, and PS65 is `KDEC002`. |

Before the clean replay, `free -h` reported **15 GiB total, 7.8 GiB available and 4.8/8.0 GiB swap used**. The largest visible resident processes were Handy (~893 MiB), Baloo (~865 MiB) and Helium (~547 MiB). The replay was serial and completed without a reported resource failure. No precise latency distribution or peak-RSS benchmark was measured; command elapsed times are coarse operational observations only. The full Python suite, corpus exports, parser regeneration, Lean, Z3, Docker, proof derivation, presumptions, criminal outcomes, sentencing and section 84/CPC work were intentionally not run. These checks provide bounded technical evidence, not proof of Haskell/reference correspondence, evidence sufficiency, legal doctrine or case outcomes.
