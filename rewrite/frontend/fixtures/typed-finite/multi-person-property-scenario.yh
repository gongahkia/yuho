typed-rules-scenario TFR02 for FictionalMultiPersonProperty-v0.2 {
  classify controls(actor:one,item:camera) as proved reason "synthetic";
  classify controls(actor:one,item:watch) as not_proved reason "synthetic";
  classify transfers(actor:one,item:camera) as proved reason "synthetic";
  classify transfers(actor:one,item:watch) as not_proved reason "synthetic";
  classify controls(actor:two,item:camera) as not_proved reason "synthetic";
  classify controls(actor:two,item:watch) as proved reason "synthetic";
  classify transfers(actor:two,item:camera) as not_proved reason "synthetic";
  classify transfers(actor:two,item:watch) as unresolved reason "synthetic unresolved";
  classify assists(actor:helper,actor:one) as proved reason "synthetic";
}
